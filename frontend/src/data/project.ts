// Single source of truth for the demo project's identity, shared between the
// toolbar display and the persistence layer so the storage key can never
// drift from what the UI shows the user.
export const PROJECT_NAME = 'Neues Projekt'
export const PROJECT_CODE = 'HDD-2024-05-23'
