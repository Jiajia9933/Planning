FROM python:3.13-slim
WORKDIR /app

COPY services/route-optimizer/pyproject.toml ./
COPY services/route-optimizer/app ./app
RUN pip install --no-cache-dir .

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
